% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pick_layer.R
\name{avg_layer}
\alias{avg_layer}
\title{多观测取中位数（行中位数合成一列）}
\usage{
avg_layer(d, prefix = "G", to = prefix, abs_max = 100)
}
\arguments{
\item{d}{data.frame / data.table}

\item{prefix}{变量前缀，默认 \code{"G"}}

\item{to}{输出列名，默认与 \code{prefix} 相同（即把结果写回 \code{G}）}

\item{abs_max}{值域控制：单板绝对值超过该阈值视为坏值置 \code{NA}（合成前逐板进行，
故单板独留的异常段也会被剔除）。默认 \code{100}（日尺度地热通量）；设 \code{Inf} 关闭。}
}
\value{
增加（或覆盖）\code{to} 列后的 \code{d}
}
\description{
把所有以 \code{prefix} 开头的列按行求\strong{中位数}（忽略 \code{NA} 与 \code{-9999} 缺测），写入
\code{to} 列。相比均值，中位数对单板异常/坏值更稳健。适用于同一深度布设的多块土壤
热通量板（HFP）或多重复观测的合成：如锦州 \code{G_8cm_1/2}、固城 \code{G_8cm_N/S}、
长白山 \code{G_0_1/2}、燕山 \code{G_2cm+G_5cm}、盘锦 \code{G_1/2/3}。
}
\examples{
# d \%>\% avg_layer("G")    # 多块热通量板 → 单列 G（行中位数，|G|>100 置 NA）
}
