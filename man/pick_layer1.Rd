% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pick_layer.R
\name{pick_layer1}
\alias{pick_layer1}
\title{挑选第一层（最浅层）变量}
\usage{
pick_layer1(d, prefix = "SM", to = prefix)
}
\arguments{
\item{d}{data.frame / data.table}

\item{prefix}{变量前缀，默认 \code{"SM"}}

\item{to}{输出列名，默认与 \code{prefix} 相同（即把第一层写回 \code{SM}）}
}
\value{
增加（或覆盖）\code{to} 列后的 \code{d}
}
\description{
在数据框 \code{d} 中找出所有以 \code{prefix} 开头的列（如 \code{SM}、\code{TS}、\code{G}），按深度
排序后取最浅的一层，写入名为 \code{to} 的列；深度无法解析时按出现顺序取第一个。
默认为加列（非破坏式），不删除其余分层。
}
\examples{
# d \%>\% pick_layer1()              # 取最浅层土壤湿度 → SM
# d \%>\% pick_layer1("TS", "TS1")   # 取最浅层土壤温度 → TS1
}
