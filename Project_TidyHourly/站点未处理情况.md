# Daily / Hourly 未处理站点说明

> 比对 `data-raw/Hourly/`、`data-raw/Daily/` 与 `data/st_flux39_names.csv` 的
> `site` 映射列得到。「未处理」= Flux/LAI 节因在 st39 中无 `site` 映射而被跳过
> （Met / 土壤节不受影响，仍正常输出）。
>
> 统计日期：2026-06-10。Hourly 34 个去重站点，Daily 40 个去重站点。

## Hourly —— 仅 1 个未处理

| 站点              | 原因                                                                                                                          |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `WSA_灌丛_燕山` | st39 中燕山只有 `YanShan-ENF`、`YanShan-MF` 两塔，**无灌丛塔行**，外部 Flux(`st39`)/LAI(`st341`) 源本身就没有它 |

“MF 乔灌混交林燕山”仅有半年数据，且原作者提供的逐日RE、GPP、ET 等变量存在明显的量级错误，因此在后续研究中弃用该站点。

## Daily —— 8 个未处理

### A. 真缺行（无法映射）

| 站点              | 原因                     |
| ----------------- | ------------------------ |
| `WSA_灌丛_燕山` | 同 Hourly，st39 无对应行 |

### B. 文件名不规范（改名即可解决）—— ✅ 已处理（2026-06-14，已重命名为 `SAV_荒漠草原_达茂_Day_FluxMet_2015-2018.csv`）

| 站点                          | 原因                                                                                                                                                                                                                                                                             |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SAV_荒漠草原_达茂_FluxMet` | 文件名为 `…_达茂_FluxMet_Day_…`，应为 `…_达茂_Day_FluxMet_…`。st39 中 `DaMao → SAV_荒漠草原_达茂` 已映射，仅因 `str_site` strip 掉 `_Day` 后残留 `_FluxMet` 而对不上 → **重命名 daily 文件即可**（建议 `SAV_荒漠草原_达茂_Day_FluxMet_2015-2018.csv`） |

### C. st39 中 `site` 列留空，但有 Daily 数据（无 Hourly 数据）—— ✅ 已处理（2026-06-14）

留空原因：这 6 个站无 hourly 文件，而 `site` 列为 Hourly/Daily 共用，当初按
hourly 缺失留空。它们的 daily 数据存在，已按下表（与实际 daily 文件名核对一致）补入：

| st39 行                 | 已填 Daily site 字符串    | 备注                                                    |
| ----------------------- | ------------------------- | ------------------------------------------------------- |
| `LuanCheng 栾城`      | `CRO_冬小麦夏玉米_栾城` |                                                         |
| `QingYang 庆阳`       | `CRO_典型粮草_庆阳`     | ✅ 已统一：daily 文件同步重命名为 `CRO_典型粮草_庆阳_Day_FluxMet_2019-2021.csv` |
| `NanDi 南荻`          | `WET_南荻湿地_洞庭湖`   |                                                         |
| `ChongMing 崇明东滩`  | `WET_崇明东滩`          |                                                         |
| `PanJin-WET 盘锦-WET` | `WET_芦苇湿地_盘锦`     |                                                         |
| `PuDing 普定`         | `WSA_退耕还林_普定`     | ⚠ 文件名 `退耕还林`，README 写 `退耕乔灌草`        |

## 没有 hourly 数据的站点（仅 daily，共 6 个）

> 2026-06-14 核对：`data-raw/Hourly/` 34 个站点键对照 st39，下列 6 行**无任何
> `_30min` 半小时文件**，只有 daily 数据。即上文 C 类 6 站——hourly 工作流本就
> 不涉及它们，本次补的 `site` 映射只为 **daily** 的 Flux/LAI 节能跑通。

| st39 行                 | site                    | 原因                                       |
| ----------------------- | ----------------------- | ------------------------------------------ |
| `ChongMing 崇明东滩`  | `WET_崇明东滩`          | 原始数据只提供逐日，无 `_30min` 文件       |
| `LuanCheng 栾城`      | `CRO_冬小麦夏玉米_栾城` | 同上（仅 `_Day_Flux` 文件）                |
| `NanDi 南荻`          | `WET_南荻湿地_洞庭湖`   | 同上                                       |
| `PanJin-WET 盘锦-WET` | `WET_芦苇湿地_盘锦`     | 同上                                       |
| `PuDing 普定`         | `WSA_退耕还林_普定`     | 同上                                       |
| `QingYang 庆阳`       | `CRO_典型粮草_庆阳`     | 同上                                       |

> 反向情况：`WSA_灌丛_燕山` **有 hourly 数据但 st39 无对应行**（燕山仅 ENF/MF
> 两塔），按计划弃用、保持跳过。

## 全覆盖待办

1. ✅ 补 C 类 6 行的 `site` 列为上表 daily 字符串。
2. ✅ 重命名达茂 daily 文件：`达茂_FluxMet_Day` → `达茂_Day_FluxMet`。
3. `WSA_灌丛_燕山` 确认外部无 Flux/LAI 源，保持跳过。
4. ✅ 庆阳统一为 `CRO_典型粮草_庆阳`：st39 与 daily 文件名已一致（2026-06-14）。

## 隐患

C 类 6 站若将来补入 hourly 数据，需注意 hourly/daily 的 `site` 字符串可能不一致
（`str_site` 对 `_30min` 与 `_Day` 的切分结果须相同）。当前它们仅有 daily，
直接填 daily 字符串安全。
